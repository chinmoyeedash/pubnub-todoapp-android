<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.todo.MainActivity"
    android:background="@color/greenblue" >

    <TextView
        android:id="@+id/tododemotextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="@string/ToDoDemo"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:background="@color/lightblue"
        style="@style/textviewstyle"
        android:padding="5dp"
        android:gravity="center_horizontal" />

    <Button
        android:id="@+id/submitbutton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="72dp"
        android:text="@string/submit"
        style="@style/darkbtn" />

    <TextView
        android:id="@+id/selecttextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/textviewstyle"
        android:layout_below="@+id/tododemotextView"
        android:layout_marginTop="94dp"
        android:text="@string/selectname"
        android:textAppearance="?android:attr/textAppearanceMedium" />

    <Spinner
        android:id="@+id/collaboratorspinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/selecttextView"
        android:layout_marginTop="16dp"
        android:entries="@array/collaborators"
        style="@style/spinnerstyle" />
    
</RelativeLayout>